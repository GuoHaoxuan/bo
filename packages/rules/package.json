{
  "name": "@bo/rules",
  "version": "0.0.0",
  "description": "Shared, deterministic 波 rules engine (resolve + data-driven skills)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}
